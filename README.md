# Next.js-Libary

Use Next Js for (SSR) Server Side Rendering 

npm install next

```bash
  "next": "^5.1.0",
  "react": "^16.3.2",
  "react-dom": "^16.3.2"
```

Create Pages and import from 'next/link'
